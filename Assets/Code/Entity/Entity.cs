using UnityEngine;


// Used to detect if target is an entity or not.
// by doing gameobject.GetComponent<Entity>();
// anything that can get exploded or shot or otherwise damaged should extend this
public class Entity : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
